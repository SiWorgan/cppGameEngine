{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Debug gameengine (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/gameengine",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "lldb",
            "preLaunchTask": "make build"
        }
    ]
}
